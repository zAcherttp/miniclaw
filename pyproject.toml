[project]
name = "miniclaw"
version = "0.1.0"
description = "Personal virtual assistant for schedule and task management"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
miniclaw = "main:main"
